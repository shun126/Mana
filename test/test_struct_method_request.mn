/*
 mana (script)
*/

struct Helper
{
    void CallOther(actor a, int b)
    {
        request(1, a->talk);
        yield();
    }
}

void Main()
{
    Helper h;
    actor a;
    h.CallOther(a, 1);
}
